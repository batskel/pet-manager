package pl.pet.manager.model;

/**
 * Action enum.
 */
public enum ActionEnum {
    /**
     * User login to the system.
     */
    SIGN_IN,
    /**
     * User logout to the system.
     */
    LOG_OUT
}
